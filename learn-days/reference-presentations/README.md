# Reference presentations — not committed

This is a **public** repo, so the bootcamp reference deck (the visual quality bar for
our slides) is **not** tracked here — `*.pptx` / `*.pdf` are gitignored.

To work against it locally, obtain the file from the deck owner (Ethan) and drop it in
this folder; it will stay ignored. See [`../SOURCES.md`](../SOURCES.md).
